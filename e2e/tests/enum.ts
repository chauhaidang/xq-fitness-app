/**
 * Enum representing muscle group IDs used in the application
 * These IDs correspond to the muscle-group-{id} containers in the form
 */
export enum MuscleGroupId {
    Chest = 1,
    Back = 2,
    Shoulders = 3,
    Biceps = 4,
    Triceps = 5,
    Forearms = 6,
    Quadriceps = 7,
    Hamstrings = 8,
    Glutes = 9,
    Calves = 10,
    Abs = 11,
    LowerBack = 12,
    Abductor = 13
}
